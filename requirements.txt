# Core
numpy>=1.26.0

# Distributed backend (Ray) — optional but recommended
ray>=2.9.0

# Web UI — optional
fastapi>=0.110.0
uvicorn>=0.27.0

# Custom protocol serialization — optional (falls back to pickle)
msgpack>=1.0.7

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
